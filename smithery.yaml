# Smithery configuration file
version: 1
startCommand:
  type: http
  configSchema:
    type: object
    properties: {} # No user configuration needed since all settings are in environment variables
build:
  dockerfile: Dockerfile 